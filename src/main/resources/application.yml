spring:
  datasource:
    username: root
    password: gengrui
    url: jdbc:mysql://localhost:3306/tdd?serverTimezone=GMT%2B8
    driver-class-name: com.mysql.jdbc.Driver
#    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  # 支持统配符 * 或者 ; 分割
  typeEnumsPackage: com.gengrui.myBatisPlusDemo.enums
  global-config:
    db-config:
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)